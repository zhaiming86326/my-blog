# Local tooling artifacts (Reasonix agent state, codegraph db)
.reasonix/
.codegraph/
reasonix.toml

# Wrangler local cache
.wrangler/

# AI 抓包原始记录 (含对话原文, 绝不允许进 git)
.capture/

# Python 缓存
__pycache__/
*.pyc

# Hugo 构建产物 (GitHub Actions 自动生成)
public/
